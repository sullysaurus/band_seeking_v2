<nav class="bg-gray-900 text-white p-4 fixed top-0 w-full shadow-lg" data-controller="navbar">
  <div class="container mx-auto flex justify-between items-center">
    
    <!-- Logo -->
    <div>
      <%= link_to "Band Seeking", root_path, class: "text-2xl font-bold tracking-wide hover:text-gray-300 transition" %>
    </div>

    <!-- Desktop Menu -->
    <div class="hidden md:flex space-x-6">
      <% if user_signed_in? %>
        <% if current_user.profile.present? %>
          <%= link_to "Profile", profile_path(current_user.profile), class: "hover:text-gray-300 transition" %>
        <% else %>
          <%= link_to "Create Profile", new_profile_path, class: "hover:text-gray-300 transition" %>
        <% end %>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "hover:text-gray-300 transition" %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, class: "bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-lg transition" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-lg transition" %>
      <% end %>
    </div>

    <!-- Mobile Menu Button -->
    <div class="md:hidden">
      <button data-action="click->navbar#toggle" class="focus:outline-none">
        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>
    </div>

  </div>

  <!-- Mobile Menu (Hidden by Default) -->
  <div data-navbar-target="menu" class="hidden md:hidden bg-gray-800 p-4">
    <% if user_signed_in? %>
      <% if current_user.profile.present? %>
        <%= link_to "Profile", profile_path(current_user.profile), class: "block py-2 text-center hover:text-gray-300 transition" %>
      <% else %>
        <%= link_to "Create Profile", new_profile_path, class: "block py-2 text-center hover:text-gray-300 transition" %>
      <% end %>
      <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "block py-2 text-center hover:text-gray-300 transition" %>
    <% else %>
      <%= link_to "Sign In", new_user_session_path, class: "block py-2 text-center hover:text-gray-300 transition" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "block py-2 text-center bg-blue-600 hover:bg-blue-700 rounded-lg transition" %>
    <% end %>
  </div>
</nav>
